# Base URL of the WIM API (no trailing slash).
# Local dev (default API on port 3000):
VITE_API_BASE_URL=http://localhost:3000/api

# Production (Render):
# VITE_API_BASE_URL=https://wimapi.onrender.com/api
